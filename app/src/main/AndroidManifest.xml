<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nougatstudio.terayahipyarhai">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/MyAppTheme">
        <activity
            android:name=".SchoolAdmin"
            android:label="@string/app_name"
            android:theme="@style/MyAppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SchoolProfile"
            android:label=" "
            android:theme="@style/MyAppTheme.NoActionBar" />
        <activity android:name=".StudentRegistration" />
        <activity android:name=".SignUp" />
        <activity
            android:name=".StudentAdmin"
            android:label="@string/title_activity_student_admin"></activity>
    </application>

</manifest>